#include <stdio.h>
int main()
{
	int x;
	scanf("%d", &x);

	int a = x / 100;
	int b = x % 100 / 10;
	int c = x % 10;
	if (c + b == 0) {printf("%d\n", a);}
	else if (c == 0) {printf("%d\n", b*10+a);}
	else printf("%d\n", c*100 + b*10 +a);

	return 0;
}
